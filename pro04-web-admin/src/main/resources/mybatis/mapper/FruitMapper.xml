<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.admin.mapper.FruitMapper">

    <!--public Fruit getFruit(Integer fid);-->
    <select id="getFruit" resultType="com.atguigu.admin.bean.Fruit">
        select * from t_fruit where fid = #{fid}
    </select>

</mapper>
